import { describe, it, expect } from 'vitest';
import { BVHBuilder } from '../src/core/BVHBuilder';

describe('BVHBuilder', () => {
  it('should build hierarchy', () => {
    expect(true).toBe(true); // Mock test for now
  });
});
